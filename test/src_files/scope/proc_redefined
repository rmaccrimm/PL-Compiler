$ Define procedure that already exists - expect 4 errors
begin
    const n = 10; integer array A[n];
    integer x; Boolean found;
    proc search 
    begin
    end;
    proc search begin
    end;
    proc i
    begin
    end;
    proc i begin end;
end.
